#include <stdio.h>
#include <stdlib.h>

int global_mode;

int main()
{
	char ch;
	int value, temp;

	global_mode = 1;
	value = MODE(ch, temp);
}

int MODE(char ch, int temp)
{
	int found = 0;

	

	return found;
}